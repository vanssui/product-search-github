{
  "name": "product-search-github-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test test/*.test.js ../backend-production-api/test/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
